<body>
  <div class="container-fluid">
    <div class="row">
      <div class="col-12 text-center">
        <h1>Stories</h1>
      </div>
      <div class="col-12">
        <div class="card articleCard">
          <div class="card-header">
            Saved Stories.
          </div>
          <div class="card-body" id="scrapedArticles">

            <tr>
              {{#each article}}
              {{> articles/saved-block}}
              {{/each}}
            </tr>

          </div>
        </div>
      </div>
    </div>
  </div>
</body>
